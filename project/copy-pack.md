# Copy pack

The voice model, plus copy that already exists and can be reused or adapted. An agent reads this alongside `PROJECT.md` before drafting anything, so what's here shapes everything downstream.

Delete the guidance in each section as you fill it in.

---

## Why this file exists

Several people and at least two AI harnesses will draft text for this project. Without a fixed reference, output drifts toward generic marketing register, and posts written in January stop sounding like posts written in June. This file is what a fresh agent with no memory of prior conversations reads before writing, so post #40 sounds like post #4.

---

## Register by audience

The same fact needs different framing depending on who's reading. Fill in how each audience should be addressed — this is the single most useful section here.

**Scientists / researchers**
> Lead with `<...>`. Never lead with `<...>`. They care about `<...>` and are put off by `<...>`.

**General / curious public**
> Lead with `<...>`. Assume no background in `<...>`.

**Designers / artists**
> Lead with `<...>`.

**Developers / open source**
> Lead with `<...>`. Link the repo, not the site.

**Funders / curators / exhibitions**
> Lead with `<...>`. Be concrete about what exists today versus what's planned — overstating maturity is the fastest way to lose a serious one.

---

## Voice samples

Two or three paragraphs per person who posts in their own name, written the way they actually write. Real samples work far better than adjectives: an agent can imitate a paragraph, but "authentic and approachable" gives it nothing.

### `<Name>` — `<their angle>`

> `<A real paragraph in their voice.>`

### `<Name>` — `<their angle>`

> `<A real paragraph in their voice.>`

---

## Standing copy

Reusable blocks. Write once, reuse everywhere, keep consistent.

**One-liner** (matches `PROJECT.md`)
> `<...>`

**Two-sentence version**
> `<...>`

**Paragraph version**
> `<...>`

**Bio line** — for when a community requires an intro post
> `<...>`

---

## Per-platform openings

The first line does most of the work, and each platform rewards a different kind. Collect ones that worked.

| Platform | Opening that works | Why |
|---|---|---|
| LinkedIn | `<...>` | `<...>` |
| Bluesky | `<...>` | `<...>` |
| Reddit self-post | `<...>` | `<...>` |
| Telegram (warm room) | `<...>` | `<...>` |
| Forum showcase | `<...>` | `<...>` |

---

## Phrases to avoid

Beyond the hard `forbidden_words` list in `PROJECT.md`, these are stylistic tells worth avoiding — LLM cadences and marketing filler that make a post read as machine-written.

- "excited to announce", "thrilled to share", "game-changing", "revolutionary"
- "it's not just X, it's Y" — a very recognisable LLM construction
- Rhetorical question openers ("Ever wondered what would happen if...")
- Hedging stacks: "it could potentially perhaps"
- "Leveraging", "harnessing", "unlocking"
- "At the intersection of" (use "where X meets Y" or just describe what it is)
- Em dashes, if your project's voice rules specify plain hyphens

---

## Copy that already worked

Paste posts that performed well, with a note on where and why. Over a few months this becomes the most valuable section in the file — it's the only record of what actually landed, as opposed to what you assumed would.

### Template

> **`<Date>`** — **`<platform>`** — outcome: **`<what followed>`**
>
> `<the post text>`
>
> Why it worked: `<...>`

(This section fills in as posts are sent and measured. After a few months it becomes the most valuable section in the file — it is the only record of what actually landed, as opposed to what you assumed would.)
