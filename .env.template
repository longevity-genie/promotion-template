# Copy to .env and fill in. Never commit .env.

# Google Sheet that scripts/sync_tracker.py edits in place.
# The id is the long string in the spreadsheet URL.
TRACKER_SHEET_ID=
# Optional Drive-title fallback when the sheet is shared with the service
# account and TRACKER_SHEET_ID is unset. Do not hardcode a title in scripts.
# TRACKER_SHEET_TITLE=

# One key per Buffer account. The prefix becomes the label in the tracker.
LINKEDIN_BUFFER_API_KEY=
# MASTODON_BUFFER_API_KEY=
# FACEBOOK_BUFFER_API_KEY=
